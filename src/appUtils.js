import { createTheme, getContrastRatio } from '@mui/material/styles';

export const appTheme = createTheme({
  palette: {
    color: {
      main: 'rgb(8 126 164 )',
      dark: '#2E3440',
      darkBody: '#ECEFF4',
    },
  },
});